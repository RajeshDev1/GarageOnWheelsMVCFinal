@model IEnumerable<GarageOnWheelsMVC.Models.ViewModel.RevenueReportViewModel>

<link rel="stylesheet" href="https://cdn.datatables.net/1.11.5/css/jquery.dataTables.min.css" />
<script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
<script src="https://cdn.datatables.net/1.11.5/js/jquery.dataTables.min.js"></script>

<table class="table table-bordered table-striped" id="revenueeTable">
        <thead>
            <tr>
                <th>Garage Name</th>
                <th>Report Date</th>
                <th>Total Revenue</th>       
            </tr>
        </thead>
        <tbody>
        @if(Model == null)
        {
            <tr>
                Record Not Found
            </tr>
        }
        else
        {
            
            @foreach (var report in Model)
                {
                    <tr>
                        <td>@report.GarageName</td>
                        <td>@report.ReportDate.ToShortDateString()</td>
                        <td>@report.TotalRevenue</td>                
                    </tr>
                }
        }
        </tbody>
    </table>

    <script>
    $(document).ready(function () {
        // Initialize DataTable
        $('#revenueeTable').DataTable();

    });

    </script>