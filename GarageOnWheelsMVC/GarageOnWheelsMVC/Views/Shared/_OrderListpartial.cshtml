@model GarageOnWheelsMVC.Models.ViewModel.OrderListViewModel

<table class="table table-bordered table-striped">
    <thead>
        <tr>
            <th class="text-center">Service Details</th>
            <th class="text-center">Order Date</th>
            <th class="text-center">Total Amount</th>
            <th class="text-center">Status</th>
            <th class="text-center">Actions</th>
        </tr>  
    </thead>
    <tbody>
        @foreach (var order in Model.Orders)
        {
            <tr>
                <td class="text-center">
                    @Html.DisplayFor(modelItem => order.ServiceDetails)
                </td>
                <td class="text-center">
                    @order.OrderDate.ToString("dd-MM-yyyy")
                </td>
                <td class="text-center">
                    @order.TotalAmount.ToString("C")
                </td>
                <td class="text-center">
                    @switch (order.Status)
                    {
                        case OrderStatus.Pending:
                            <div>
                                <i class="fas fa-hourglass-half"  title="Pending" style="font-size: 14px; color: #f39c12;"></i>
                                Pending
                            </div>
                            break;
                        case OrderStatus.Completed:
                            <div>
                                <i class="fas fa-check-circle"  title="Completed"  style="font-size: 14px; color: #2ecc71;"></i>
                                Completed
                            </div>
                            break;                     

                    }
                </td>
                <td class="text-center">
                    <a asp-action="Edit" asp-controller="Order" asp-route-id="@order.Id" title="Edit">
                        <i class="fas fa-edit"></i>
                    </a> |

                    <a asp-action="Delete" asp-controller="Order" asp-route-id="@order.Id" title="Delete" class="text-danger" onclick="return confirmDelete();">
                        <i class="fas fa-trash-alt"></i>
                    </a>
                </td>
            </tr>
        }
    </tbody>
</table>


<script>
    function confirmDelete() {
        return confirm("Are you delete this order?");
    }
</script>
