@model GarageOnWheelsMVC.Models.ViewModel.GarageViewModel

@{
    bool isEdit = Model.Id != Guid.Empty;
    ViewData["Title"] = isEdit ? "Edit Garage" : "Create Garage";
}

<h2 class="text-center mb-4">@ViewData["Title"]</h2>

<div class="container">
    <div class="row justify-content-center">
        <div class="col-md-8">
            <form asp-action="Save" method="post" class="needs-validation">

                @if (isEdit)
                {
                    <input type="hidden" asp-for="Id" />
                }

                <div class="form-group mb-3">
                    <label asp-for="Name" class="form-label">Name</label>
                    <input asp-for="Name" class="form-control" placeholder="Enter garage name" required />
                    <span asp-validation-for="Name" class="text-danger"></span>
                </div>

                @if (User.IsInRole("SuperAdmin"))
                {
                    <div class="row">
                        <div class="col-md-6 mb-4">
                            <label asp-for="UserId" class="form-label">Owner Name</label>
                            <select id="userid" asp-for="UserId" class="form-select form-select-lg" placeholder="Select GarageOwner" required>
                                <option value="">Select Garage Owner</option>
                            </select>
                        </div>
                    </div>
                }
                else
                {
                    <input asp-for="UserId" value="@Guid.Empty" hidden />
                }

                
                <div class="form-group mb-3">
                    <label asp-for="Address" class="form-label">Address</label>
                    <input asp-for="Address" class="form-control" placeholder="Enter garage address" required />
                    <span asp-validation-for="Address" class="text-danger"></span>
                </div>

                <div class="row">
                    <div class="col-md-6 mb-4">
                        <label asp-for="CountryId" class="form-label">Country</label>
                        <select id="country" asp-for="CountryId" class="form-select form-select-lg" required>
                            <option value="">Select Country</option>
                        </select>
                        <span asp-validation-for="CountryId" class="text-danger"></span>
                    </div>

                    <div class="col-md-6 mb-4">
                        <label asp-for="StateId" class="form-label">State</label>
                        <select id="state" asp-for="StateId" class="form-select form-select-lg" required>
                            <option value="">Select State</option>
                        </select>
                        <span asp-validation-for="StateId" class="text-danger"></span>
                    </div>
                </div>

                <div class="row">
                    <div class="col-md-6 mb-4">
                        <label asp-for="CityId" class="form-label">City</label>
                        <select id="city" asp-for="CityId" class="form-select form-select-lg" required>
                            <option value="">Select City</option>
                        </select>
                        <span asp-validation-for="CityId" class="text-danger"></span>
                    </div>

                    <div class="col-md-6 mb-4">
                        <label asp-for="AreaId" class="form-label">Area</label>
                        <select id="area" asp-for="AreaId" class="form-select form-select-lg" required>
                            <option value="">Select Area</option>
                        </select>
                        <span asp-validation-for="AreaId" class="text-danger"></span>
                    </div>
                </div>


                <button type="submit" class="btn btn-primary w-100">
                    @if (isEdit)
                    {
                        <text>Update Garage</text>
                    }
                    else
                    {
                        <text>Create Garage</text>
                    }
                </button>
            </form>
        </div>
    </div>
</div>

@section Scripts {
    <script>
        $(document).ready(function () {
            var $userIdSelect = $('#userid');

            // Get the current user ID from the model
            var currentUserId = '@Model.UserId';

            // Function to populate the garage owners dropdown
            function populateGarageOwners() {
                $.getJSON('@Url.Action("GetAllGarageOwners", "User")')
                    .done(function (users) {
                        console.log(users);  // Logging users array
                        console.log(currentUserId);  // Logging the current user ID

                        // Loop through the users and populate the dropdown
                        $.each(users, function (index, user) {
                            $userIdSelect.append(
                                $('<option>', { value: user.Id, text: user.FirstName + ' ' + user.LastName })
                            );
                        });

                        // Set the selected value for garage owner after dropdown is populated
                        if (currentUserId !== '00000000-0000-0000-0000-000000000000') {
                            $userIdSelect.val(currentUserId);  // Set selected value to the current user
                        }
                    })
                    .fail(function () {
                        alert("Error fetching garage owners.");
                    });
            }

            populateGarageOwners();
           
        });
    </script>
    


    <script src="~/js/cascadingddl.js"></script>
    <script src="~/lib/jquery/dist/jquery.min.js"></script>
    <script src="~/lib/bootstrap/dist/js/bootstrap.bundle.min.js"></script>
    <partial name="_ValidationScriptsPartial" />
}
